import 'package:flutter/material.dart';

const pt10 = Padding(padding: EdgeInsets.only(top: 10));
const pt20 = Padding(padding: EdgeInsets.only(top: 20));
const pt30 = Padding(padding: EdgeInsets.only(top: 30));
const pl10 = Padding(padding: EdgeInsets.only(left: 10));
const pl20 = Padding(padding: EdgeInsets.only(left: 20));
const pl30 = Padding(padding: EdgeInsets.only(left: 30));
const pa10 = Padding(padding: EdgeInsets.all(10));
const pa20 = Padding(padding: EdgeInsets.all(20));
const pa30 = Padding(padding: EdgeInsets.all(30));